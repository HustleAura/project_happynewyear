import 'package:flutter/material.dart';

class DiaryHome extends StatelessWidget {
  const DiaryHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is the diary home page'),
      ),
    );
  }
}
